const KEYS = {
    ENTER: 13,
    ESCAPE: 27,
}

const MOBILE_WIDTH = 632;
const FIRST_ITEM = 0;
const CLIENT_WIDTH = document.documentElement.clientWidth;
const SWIPE_OFFSET = (CLIENT_WIDTH > 400) ? 200 : 150;
const MOBILE_TOP_OFFSET = 112;